server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lms_quiz_db?createDatabaseIfNotExist=true
    username: root
    password: Papanis@1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect