spring:
  application:
    name: LMS Certificate Service
  datasource:
    url: jdbc:mysql://localhost:3306/lms_certificate_db?useSSL=false&serverTimezone=UTC
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  server:
    port: 8087