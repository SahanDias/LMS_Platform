server:
  port: 8083

spring:
  application:
    name: video-service
  datasource:
    url: jdbc:mysql://video-service-db.czussyky8e0l.eu-north-1.rds.amazonaws.com:3306/video-service-db?allowPublicKeyRetrieval=true&useSSL=false
    username: video_user
    password: video123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

enrollment:
  service:
    url: http://localhost:8081

progress:
  service:
    url: http://localhost:8082

contentflow:
  service:
    url: http://localhost:8084